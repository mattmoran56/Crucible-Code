{
  "name": "codecrucible-relay-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 9000",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types": "wrangler types",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.10",
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.7",
    "wrangler": "^3.95.0"
  }
}
